# KeeBind: Project State

_Last updated: 2026-07-26_

## Status: v0.2.8, feature-complete v1, macOS-verified

| Feature | Status | Notes |
|---|---|---|
| Tray / menu-bar residency | ✅ Done | macOS menu bar (template icon), Windows notification area. Native hover text explains left-click and right-click actions; Windows taskbar hover uses the main window title. Close hides the window; Quit is in the tray menu (right-click). |
| Pinned bindings | ✅ Done | Star a binding to pin it. Left-clicking the tray icon opens a searchable popover. Search filters dynamically by name, description, hotkey, action and workflow target. Double-click a row to run it, or use the Run and Manage buttons that appear on hover. |
| About | ✅ Done | About tab in the sidebar, plus a standalone window from the tray menu. Version, description, OS and runtime versions. |
| Dock / taskbar icon | ✅ Done | Shown by default (no `LSUIElement`); "Show in Dock" in Settings hides it for menu-bar-only use. Same mark as the tray and installer. |
| Bounded window resizing | ✅ Done | Main window remains draggable and resizable from 940×600 through 1200×800. Maximize/full-screen cannot bypass the ceiling, the application shell always fills the viewport, the main document never exposes a horizontal scrollbar, and Bindings, Key Listener and Settings stay exactly the same width. |
| Brand + icon set | ✅ Done | Custom KeeBind mark: a keycap on an indigo squircle with a split "K". Generated by `npm run icons`; `components/Logo.tsx` mirrors it as inline SVG. |
| Light/Dark theme | ✅ Done | Defaults to system; overridable in Settings. `nativeTheme.themeSource` → CSS `prefers-color-scheme`. |
| Key Listener | ✅ Done | `uiohook-napi` global hook, opt-in start/stop. On macOS it requires Accessibility, matching uiohook's active event tap and AX trust check. Input Monitoring is not required. Renders whole chords as keycaps or opens a new binding with that accelerator prefilled. |
| Hotkey → action bindings | ✅ Done | Electron `globalShortcut`. Bindings have separate name and description fields. Actions: launch app, open URL, open path, shell command, multi-step workflow with per-step delays. Existing bindings edit in a focused modal with explicit Save/Discard actions; new bindings retain their inline creation flow. Each binding can require a fail-closed confirmation before global or manual runs. Dynamic search filters by name, description, hotkey, action and target. Deleting requires confirmation. Per-binding enable toggle, Run button and master switch. |
| Conflict warnings | ✅ Done | Per-OS databases (`src/main/data/conflicts/darwin.json`, `win32.json`) incl. researched F13 to F24 guidance; also warns on duplicate in-app bindings. Checked live while editing a binding. |
| macOS permissions | ✅ Done | Accessibility is the sole permission because uiohook uses an active `kCGEventTapOptionDefault`; Apple confirms Accessibility includes event listening. Input Monitoring UI, bridge, request and reset paths were removed. Request shows macOS's confirmation dialog and leaves opening System Settings to its explicit Open System Settings choice; status refreshes once per second/on focus. |
| Tooltips everywhere | ✅ Done | Portal-rendered so nothing clips them; auto-flip and viewport clamping. `Tooltip` / `title` on all actionable controls, incl. tray menu items. |
| Focus-ring suppression | ✅ Done | Electron/macOS native accent outlines—including the thick yellow/orange halo—are disabled throughout the renderer for buttons, navigation, inputs, selects, toggles and links. |
| Launch at login | ✅ Done | Works in packaged builds only (macOS restriction for unsigned/ad-hoc dev builds). |
| Packaging | ✅ Verified | electron-builder: mac dmg+zip arm64 (ad-hoc signed) and win NSIS x64 both build from this Mac (`npmRebuild: false` + bundled N-API prebuilds make the cross-build work). |

## Verified

- macOS (this machine): all current views against the browser dev mock in light and dark, typecheck and production bundle clean.
- Binding editor: clicking Capture changes the label to "Press a key" with **zero** layout movement (panel height, field positions and widths all identical before and after). This was the reported bug.
- Existing-binding editor modal: Edit opens a viewport-centered `aria-modal` dialog with Name focused and keyboard focus trapped inside. Discard leaves the stored binding unchanged; Save persists and closes. At 940×600, a seven-step workflow scrolls inside the modal (`scrollHeight 727 > clientHeight 566`) with no horizontal overflow, while Add binding still uses the separate inline creation editor.
- Bindings search: typing filters immediately across names, descriptions, accelerators, action summaries, workflow steps and targets; result count, clear action and no-match state update with it.
- Confirm-before-run: legacy bindings run without prompting; checked bindings show one native dialog containing the hotkey, name, description and complete action. Deny performs nothing, acceptance runs once, and two simultaneous requests share one prompt/action. A binding disabled while the prompt is open is denied even if the stale prompt is accepted. A global hotkey fired while the main window is hidden brings the owned prompt to the front and restores the window to hidden after denial. The Pin and Confirm options plus separate Name/Description fields fit at the 940px minimum without horizontal overflow.
- Delete confirmation: Cancel, Escape and backdrop dismissal preserve the binding; confirming removes only the selected binding and prevents double submission while in progress.
- Workflow steps: a two-step workflow with Browse buttons fits the pane with no horizontal overflow (`scrollWidth === clientWidth`), and the Save button stays on screen. The step row wraps rather than pushing the pane wider.
- Main window: native minimum/maximum constraints are 940×600 and 1200×800; every main view retains `scrollWidth === clientWidth` at both limits. Vertical content scrolling remains available for long bindings and settings.
- Tab alignment: Bindings, Key Listener and Settings have identical left/right edges at maximum size, including when Settings is forced to overflow vertically; its scrollbar uses the gutter already reserved by the other views.
- Post-permission transition: with the stale-record guidance present, removed, Settings remounted, and Key Listener visited between those states, the app shell stays 1200px and each functional view stays 958px wide. This reproduces the reported packaged-app sequence rather than only checking a fresh renderer.
- Focus styling: sidebar navigation, Settings select and checkbox, binding text input and binding action button all remain programmatically focusable while reporting `outline-style: none` and no focus box shadow, including in `:focus-visible`.
- Tray popover at 320px: search auto-focuses and filters immediately by name, description, accelerator and full action/workflow data. Mixed-case multi-term search, result count, no-match state, Clear and Escape recovery pass with no horizontal overflow; the popover reports new heights as results change.
- About window at 420x540: content fits without clipping the buttons.
- Key Listener: shows a single green "permissions granted" line, or a warning line with a "Fix in Settings" button that lands on Settings.
- Listener-to-binding handoff: after capturing an expressible key or chord, Create binding switches to Bindings and opens a fresh editor with the accelerator already filled in.
- Technical key details: default listener view hides numeric codes, accelerator strings/copy and their history columns; the Settings toggle restores all of them for diagnostics.
- Permission panel: one Accessibility row and one Request permission button. The stale-grant branch renders above it with a "Clear old records" button; the manual-steps block stays collapsed until asked for.
- Permission audit: libuiohook source uses `kCGEventTapOptionDefault` and checks `AXIsProcessTrusted`; Apple DTS confirms Accessibility grants both posting and listening, while Input Monitoring grants listening only. There is no order-of-operations requirement and no reason to request both.
- Installed-copy diagnosis: `/Applications/KeeBind.app` and both mounted KeeBind DMGs were still v0.2.0 and contained no Core Graphics bridge. The reported `not checked yet` state therefore came from the old code path, not the v0.2.1 artifact.
- Popover icons: Run and Manage measure identically (26x26 button, 14x14 svg). Double-clicking a row calls `runBinding` with that row's id.
- Chord capture: ⌘⌃⇧F records as **one** history row with accelerator `Command+Control+Shift+F`; overlapping unmodified keys stay separate rows; a bare modifier tap is captured with no accelerator; Clear empties display and history.
- Tooltips: bubble portals to `<body>`, stays inside the viewport (`x: 8` clamp) and flips below the anchor at the top of the sidebar, which is the case that used to be cut off.
- Modal tooltips: the portal layer renders above Edit and Delete backdrops, so tooltips for Save, Discard, Cancel and Delete remain visible rather than being painted behind the modal.
- Accessibility request sequencing: Request permission invokes only macOS's native Accessibility prompt. KeeBind no longer opens the privacy pane in parallel; System Settings opens only if the user chooses Open System Settings in that prompt, while the manual recovery link remains available separately.
- Packaging identity (the fix for the privacy-pane naming): `codesign -dv` on the packaged app reports `Identifier=com.keebind.app`, Info.plist bound, 252 sealed resources, and `codesign --verify --deep --strict` passes. Was `Identifier=Electron`, Info.plist not bound, no sealed resources.
- Artifacts: `KeeBind-0.2.8-arm64.dmg` and `KeeBind-0.2.8-arm64-mac.zip` produced and checksum-verified; the bundle contains no obsolete Input Monitoring bridge/code and passes `codesign --verify --deep --strict`. The latest Windows artifact remains `KeeBind Setup 0.2.0.exe` (x64).

## The stale permission problem (diagnosed 2026-07-23)

Reported symptom: System Settings showed KeeBind ticked under Accessibility while KeeBind reported no access, and the entry survived trashing the app.

`codesign -d -r- KeeBind.app` gives the answer:

```
# designated => cdhash H"9d5278bbd14c12fde4f9fd8244436a392b741b43"
```

That is the entire designated requirement. No identifier, no certificate, because an ad-hoc signature has neither. macOS stores this requirement when a permission is granted, and the cdhash is a hash of the binary, so **every rebuild produces a different one and silently invalidates the grant** while the pane keeps showing the old row as enabled. TCC records are keyed by bundle id and outlive the bundle, so deleting the app leaves the record behind.

KeeBind now records the cdhash it was granted under, reports `staleGrant` when the running build differs, explains it, and offers a one-click `tccutil reset Accessibility`. **The permanent fix is a Developer ID certificate**, whose requirement is certificate-based and survives rebuilds. Until then a re-grant is needed after each update.

## Not yet verified (open items)

- **The packaged app has not been launched with a fresh Accessibility grant.** Installing the DMG and granting Accessibility is the final end-to-end listener test.
- **The tray popover has only been seen in a browser window.** Its rendering and hover behaviour are verified, but the parts that need a real `Tray` (positioning under the menu-bar icon, hide-on-blur, left-click vs right-click) have not run in Electron yet.
- **Browse dialogs** are wired and typechecked, but `dialog.showOpenDialog` cannot run in the browser harness, so the mock returns a canned path.
- **Windows runtime**: tray behavior, taskbar icon, conflict DB accuracy, `start`-based app launching, NSIS install. Needs a Windows machine.
- **The Accessibility request has not been exercised against a fresh real TCC prompt.** Doing so would change the user's current privacy records, so the packaged handoff leaves that final grant to the user.
- **`tccutil reset` was not run on this machine.** The syntax is confirmed (`tccutil reset SERVICE [BUNDLE_ID]`) but running it would have cleared the user's own records, so it was left alone.

## Planned / deferred (phase 2)

- **OS-level key-to-key interception** (e.g. CapsLock→Esc system-wide). Deliberately deferred, see DECISIONS.md #3. Would need a native N-API module: `WH_KEYBOARD_LL` + `SendInput` (Windows), `CGEventTap` + repost (macOS).
- **Developer ID signing + notarization.** The current ad-hoc signature is content-based, so macOS permission grants reset on every update (DECISIONS.md #8). A real certificate fixes that and removes the Gatekeeper warning.
- Auto-update (electron-updater) once there's a release channel.
